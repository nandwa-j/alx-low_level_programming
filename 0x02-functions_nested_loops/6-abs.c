#include "main.h"
#include <stdio.h>
/**
 * _abs - Computes the absolute value of an integer
 * @i: int type number
 * 
 * Returns: The absolute value of @i
 * main - Entry
 */
int _abs(int i)
{
int i;
if (i < 0)
{
return (i * -1);
}
else
{
return (i);
}

int main(void)
{
}
