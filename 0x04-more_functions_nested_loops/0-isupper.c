#include "main.h"
#include <stdio.h>
/**
 * _isupper - checks for uppercase  letter
 * @c: type int number
 * Return: 1 if c is uppercase. 0 otherwise
 */
int _isupper(int c)
{
c = 65;
if (c < 91)
{
return (1);
}
else
{
return (0);
}
}
